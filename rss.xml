---
layout: empty
permalink: /rss.xml
---
<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en">
<title type="html">{{ site.title }}</title>
<subtitle type="html">{{ site.description }}</subtitle>
<generator uri="https://github.com/mojombo/jekyll">Jekyll</generator>
<link rel="self" type="application/atom+xml" href="{{ site.url }}/feed.xml" />
<link rel="alternate" type="text/html" href="{{ site.url }}" />
<updated>{{ site.time | date_to_xmlschema }}</updated>
<id>{{ site.url }}/</id>
<author>
  <name>{{ site.owner.name }}</name>
  <email>{{ site.owner.email }}</email>
  <uri>{{ site.url }}/</uri>
</author>
{% for post in site.posts %}

<entry>
  <title type="html"><![CDATA[{{ post.title | cdata_escape }}]]></title>
 <link rel="alternate" type="text/html" href="{% if post.link %}{{ post.link }}{% else %}{{ site.url }}{{ post.url }}{% endif %}" />
  <id>{{ site.url }}{{ post.id }}</id>
  {% if post.modified %}<updated>{{ post.modified | to_xmlschema }}T00:00:00-00:00</updated>
  <published>{{ post.date | date_to_xmlschema }}</published>
  {% else %}<published>{{ post.date | date_to_xmlschema }}</published>
  <updated>{{ post.date | date_to_xmlschema }}</updated>{% endif %}
  <author>
    <name>{{ post.cited | default: site.owner.name }}</name>
    <uri>{{ post.link   | default: post.url }}</uri>
  </author>
  <content type="html">{{ post.content | xml_escape }}
  {% assign min = 1 %}
  {% assign max = site.data.offers.size %}
  {% assign diff = max | minus: min %}
  {% assign item = "now" | date: "%N" | modulo: diff | plus: min %}
  {% assign offer = site.data.offers[item] %}
  <![CDATA[<br/><p>Notes: Thanks for keeping RSS alive. If you have an RSS feed please send it my way.</p><p>Here is a special sponsor/advert link just for you.</p><p><a rel="sponsored" href="{{offer.link}}">{{offer.title}}</a> [T&Cs apply].</p>]]>

  </content>
</entry>
{% endfor %}
</feed>