name: Deploy to GitHub Pages

on:
  push:
    branches:
        - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.4'
          bundler-cache: true

      - name: Build Jekyll site
        run: bundle exec jekyll build

      - name: Copy markdown source files
        run: |
          find _posts -name "*.md" -o -name "*.markdown" | while read file; do
            filename=$(basename "$file")
            if [[ $filename =~ ^([0-9]{4})-([0-9]{2})-([0-9]{2})-(.+)\.(md)$ ]]; then
              year="${BASH_REMATCH[1]}"
              month="${BASH_REMATCH[2]}"
              day="${BASH_REMATCH[3]}"
              slug="${BASH_REMATCH[4]}"
              dest="_site/${year}-${month}-${day}-${slug}.md"
              mkdir -p "$(dirname "$dest")"
              cp "$file" "$dest"
              echo "Copied $file -> $dest"
            fi
          done

          # Copy page markdown files (about/index.md -> about.md, etc.)
          for dir in about press speaking; do
            if [[ -f "${dir}/index.md" ]]; then
              cp "${dir}/index.md" "_site/${dir}.md"
              echo "Copied ${dir}/index.md -> _site/${dir}.md"
            fi
          done

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4